import styled from "styled-components";
import { colors } from "../variables";

const Text = styled.p`
  color: ${colors.blue};
  font-size: 17px;
  margin-top: 15px;
  margin-bottom: 30px;
`;

export default Text;
